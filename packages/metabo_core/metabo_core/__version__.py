"""Single authoritative version source for the METRA platform.

Every package, app, GUI About dialog, and CLI --version flag must derive
its version from this file (directly or via re-export).
"""

__version__ = "1.2.260721"
